PROJECT: EngPulse AI — Engineering Org Intelligence Command Center
I'm building this project with you, module by module, from the PRD we created. I am an AI engineer building this for my portfolio, and I need to deeply understand every piece — so we build incrementally with a verification checkpoint after each module. Do not build ahead.
Our working agreement — follow this for every module:

Before coding: Tell me what this module does, its inputs and outputs, where it fits in the pipeline, and the 2–3 key design decisions. Wait for my "go" before writing code.
Build one module at a time only. Never start the next module until I've verified the current one and explicitly approved moving on. If a module is large, break it into sub-steps and checkpoint each.
For each module, deliver:

The actual code, clearly organized into files
A runnable verification step — an exact command I type plus realistic sample/synthetic data — so I can run it myself and see real output without waiting on live APIs
The expected output, so I know what "working" looks like
The unit tests for that module and how to run them
A short plain-language explanation of how it works, so I can defend it in an interview


Then stop and wait. End every module with: a one-line summary of what we built, the command to verify it, and the question "Does the output look right — ready for the next module?" Do not proceed until I confirm.
Keep a running build log: maintain a short PROGRESS.md listing modules done, the verification command for each, and what's next — so we never lose the thread across sessions.

Tech context (already decided):

Stack: Python + FastAPI, PostgreSQL + pgvector, Redis + Celery/RQ, React/Next dashboard, Docker, self-hosted Langfuse for tracing, pydentic
AI: self-hosted Ollama (chat + embeddings, OpenAI-compatible endpoint) behind a model-agnostic interface; everything runs free/zero-cost
Tracker: Linear. Dashboard: in the MVP.
Core principle: deterministic metrics are computed in code; the LLM only retrieves, reasons, prioritizes, and summarizes — grounded in real records, never inventing numbers.

Build order (from the PRD milestones):

Scaffold — repo structure, config (pointing at my Ollama endpoint), Postgres schema, one GitHub connector reading one public repo end-to-end
Ingestion & Normalization + entity resolution + test fixtures
Metrics & Detectors (PR-flow, CI/test-health, delivery-drift) + the eval harness
Knowledge-silo RAG (index, hybrid search, reranking) + grounded synthesis
Ask EngPulse agent
Scoring engine + alert router + digests
API + dashboard + full tracing
Eval harness completion + README with headline metrics

RULES:
1. update progress.md file after completion every module. It represent to progress status.
2.read PRD and starter-prompt.txt text file  for every new chat session. and start from ta recommended module using the these project insight.
3. start form the next of progression which will be written in progress.md file